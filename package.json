{
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "npm run clean && node scripts/generate-config.js",
    "build": "node scripts/build.js",
    "start": "http-server . --cors -c-1",
    "dev": "node scripts/build.js --watch"
  },
  "dependencies": {
    "@types/dotenv": "^8.2.3",
    "dotenv": "^17.2.3",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "esbuild": "^0.25.10",
    "rimraf": "^6.0.1"
  }
}
