{
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "npm run clean && ts-node scripts/generate-config.ts",
    "build": "ts-node scripts/build.ts",
    "build:docker": "npm run clean && node scripts/generate-config.js && node scripts/build.js",
    "start": "http-server . --cors -c-1",
    "dev": "ts-node-dev --respawn scripts/build.ts"
  },
  "dependencies": {
    "@types/dotenv": "^8.2.3",
    "dotenv": "^17.2.3",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "esbuild": "^0.25.10",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0"
  }
}
